import { ContentPage } from '@/app/[slug]/page'

export const metadata = { title: 'Muhurat', description: 'A basic, respectful guide to traditional Muhurat time windows.', alternates: { canonical: '/muhurat' } }

export default function MuhuratPage() { return <ContentPage slug="muhurat" /> }
