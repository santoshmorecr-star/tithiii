import { SiteShell, AdSlot } from '@/components/site-shell'
import { getPanchang, details, timings, observances } from '@/lib/panchang'
import { MapPin, Moon, Sun } from 'lucide-react'
export const metadata={title:'Today’s Panchang',description:'Today’s Tithi, Nakshatra, Yoga, Karana and important timings for Mumbai.'}
export default async function Panchang(){const p=await getPanchang(new Intl.DateTimeFormat('en-CA',{timeZone:'Asia/Kolkata'}).format(new Date()));return <SiteShell><main className="mx-auto max-w-6xl px-5 pb-16 pt-12"><div className="flex flex-wrap items-center justify-between gap-4"><div><p className="text-xs font-semibold uppercase tracking-[.22em] text-primary">Daily Panchang</p><h1 className="mt-3 font-serif text-5xl tracking-tight">Today&apos;s Panchang</h1><p className="mt-3 text-muted-foreground">Friday, 15 August 2026 · <span className="inline-flex items-center gap-1"><MapPin size={14}/> Mumbai, India</span></p></div><div className="rounded-full bg-secondary px-4 py-2 text-xs text-secondary-foreground">Asia/Kolkata · IST</div></div><AdSlot slot="6459216650" /><section className="mt-10 grid gap-5 md:grid-cols-2"><div className="rounded-3xl bg-[#293c4b] p-7 text-[#fffaf2]"><p className="text-xs uppercase tracking-[.22em] text-[#d9ad63]">Tithi today</p><h2 className="mt-7 font-serif text-4xl">Shukla Panchami</h2><p className="mt-3 text-sm text-[#c9d3d6]">Shukla Paksha · until 03:48 PM</p><div className="mt-10 border-t border-white/15 pt-5 text-sm text-[#c9d3d6]">Timings are calculated for Mumbai using IST.</div></div><div className="grid grid-cols-2 gap-3">{timings.map(t=><div key={t.label} className="rounded-2xl border border-border bg-card p-5"><div className="flex justify-between"><span className="text-sm text-muted-foreground">{t.label}</span>{t.label.includes('Sun')?<Sun className="text-primary" size={18}/>:<Moon className="text-primary" size={18}/>}</div><p className="mt-8 font-serif text-2xl">{t.value}</p></div>)}</div></section><div className="mt-12 grid gap-10 md:grid-cols-[1.2fr_.8fr]"><section><h2 className="font-serif text-2xl">Panchang elements</h2><div className="mt-4 grid grid-cols-2 gap-3 sm:grid-cols-3">{details.map(d=><div className="rounded-2xl border border-border bg-card p-4" key={d.label}><p className="text-xs text-muted-foreground">{d.label}</p><p className="mt-2 text-sm font-semibold">{d.value}</p><p className="mt-1 text-xs text-muted-foreground">{d.note}</p></div>)}</div></section><section><h2 className="font-serif text-2xl">Important windows</h2><div className="mt-4 grid gap-2">{observances.map(o=><div className="rounded-xl border border-border bg-card p-4" key={o.title}><p className="text-sm font-semibold">{o.title}</p><p className="mt-1 text-xs text-muted-foreground">{o.time}</p></div>)}</div></section></div><p className="mt-12 max-w-2xl text-xs leading-6 text-muted-foreground">Panchang timings can vary by location, calculation method and regional tradition. This page is informational and does not make religious claims as scientific facts.</p></main></SiteShell>}
